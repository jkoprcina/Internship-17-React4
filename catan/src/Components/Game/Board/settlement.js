import React from "react";
import "../../../Css/Board/settlement.css";

const Settlement = ({ index, type, isCity }) => {
  return <div className={"crossroad " + type} />;
};

export default Settlement;
